# cruksi/multiqc tests

Provide tiny staged files that MultiQC can read (e.g., one FastQC HTML/ZIP).
Alternatively, create a small dummy file and ensure the module runs without error
when scanning the current directory. Adjust tests as needed for your pipelines.
